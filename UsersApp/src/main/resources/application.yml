server:
  port: 8080

spring:
  application:
    name: usersapp
  datasource:
    username: teamtico
    password: password
    url: jdbc:postgresql://localhost:5432/userDB
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true